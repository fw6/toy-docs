/**
 * @typedef {import('@tiptap/core').NodeWithPos} NodeWithPos
 * @typedef {import('@tiptap/core').Predicate} Predicate
 *
 * @typedef {import('@tiptap/pm/state').Transaction} Transaction
 * @typedef {import('@tiptap/pm/state').EditorState} EditorState
 * @typedef {import('@tiptap/pm/state').SelectionBookmark} SelectionBookmark
 * @typedef {import('@tiptap/pm/state').PluginKey} PluginKey
 * @typedef {import('@tiptap/pm/state').Command} Command
 *
 * @typedef {import('@tiptap/pm/view').Decoration} Decoration
 * @typedef {import('@tiptap/pm/view').DecorationSet} DecorationSet
 * @typedef {import('@tiptap/pm/view').EditorView} EditorView
 *
 * @typedef {import('@tiptap/pm/model').Node} PMNode
 * @typedef {import('@tiptap/pm/model').Slice} Slice
 * @typedef {import('@tiptap/pm/model').Attrs} Attrs
 * @typedef {import('@tiptap/pm/model').ResolvedPos} ResolvedPos
 * @typedef {import('@tiptap/pm/model').NodeType} NodeType
 * @typedef {import('@tiptap/pm/model').Schema} Schema
 *
 * @typedef {import('@tiptap/pm/transform').Mapping} Mapping
 * @typedef {import('@tiptap/pm/transform').Mappable} Mappable
 *
 * @typedef {import('./typings').TableOptions} TableOptions
 * @typedef {import('./typings').ParagraphOptions} ParagraphOptions
 */
