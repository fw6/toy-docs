/**
 * @typedef {import('@tiptap/core').Editor} Editor
 * @typedef {import('@tiptap/core').Content} Content
 * @typedef {import('@tiptap/core').Extensions} Extensions
 *
 * @typedef {import('svelte').ComponentType} ComponentType
 *
 * @typedef {import('@storybook/svelte').Meta<EditorComponent>} EditorCmpMeta
 * @typedef {import('@storybook/svelte').StoryObj<EditorComponentProps>} EditorCmpStoryObj
 *
 * @typedef {import('./types').EditorComponentProps} EditorComponentProps
 * @typedef {import('./types').EditorToolbarProfile} EditorToolbarProfile
 * @typedef {import('./types').EditorComponent} EditorComponent
 *
 */
